# App
NODE_ENV=development
PORT=3000

# Database (SQL Server)
DB_HOST=sqlserver
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=ParqueRM_2026_StrongPass!
DB_NAME=ParqueRM
DB_ENCRYPT=false
DB_TRUST_SERVER_CERT=true

# JWT
JWT_SECRET=parque_rm_super_secret_local_2026_access
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=parque_rm_super_secret_local_2026_refresh
JWT_REFRESH_EXPIRES_IN=7d

# Admin bootstrap (only used if ADMIN_BOOTSTRAP=true)
ADMIN_BOOTSTRAP=false
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin2026!
ADMIN_FULL_NAME=Administrador
ADMIN_EMAIL=admin@parquerm.local

# CORS
CORS_ORIGIN=http://localhost:5173

# Rate limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100
AUTH_THROTTLE_TTL=60
AUTH_THROTTLE_LIMIT=5

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
